String[] 这是一个array
String  这是一个object
什么是interface：



instance variable, fields, class variable 区别: fields 是指所有的variable
Class Texas {
  public int x;       
}                   //instance variable , x会存在Texas这个object里，也就是dynamic里

Class Texas {
  public static int x;
}                 // class variable， x会存在static里
uber u = new uber(), 这里u存在stack里，u是一个pointer，uber存在dynamic里。除了static，所有的fields都在stack里


break：跳出loop，直接运行loop之后的第一行
return：回到caller

throw: 出错就return，有点像return的功能
Exception e: 错误被识别到后，e会被当做pointer指向比如illegalargumentexception这个object，之后就可以对e进行e.gettext()之类的操作

thread 拿不到lock的两种情况： 在outsider queue里，在inside queue里但是wait（）状态
thread被notifyall（）之后只能one by one的复活



'' 是char，且char不能为空的
“” 是string，string可以为空的



public int s = 0 和 int s = 0 的区别： 这两个一样，第二个只是省略了public
什么是cast
thorws exception 执行原理？

array 没有toString函数，所以想要print array必须用Arrays.toString(array)；

Interface 的作用是可以限制能被call的函数。比如在server端implement一个interface，这样的话client端只能
call和interface里边一样的server端函数

Interface 的作用是可以限制能被call的函数。比如在server端implement一个interface，这样的话client端只能
Interface 的作用是可以限制能被call的函数。比如在server端implement一个interface，这样的话client端只能
